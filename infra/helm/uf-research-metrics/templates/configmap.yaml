apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ufrm.fullname" . }}-config
  labels:
    {{- include "ufrm.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
